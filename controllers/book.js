const { Book, Author, Genre } = require('../models/Book')
const request = require('request')

module.exports = {
    show: () => {
//show one particular book
    },
    index: ()=>{
//show all books

    }
}